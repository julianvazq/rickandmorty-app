import styled from 'styled-components';

export const Count = styled.div`
    border-radius: 50px;
    color: #fff;
    background: var(--blue);
    padding: 0 0.25rem;
    font-weight: 700;
`;
